//inputs

key_up = keyboard_check(vk_up);
key_down =keyboard_check(vk_down);
key_right = keyboard_check(vk_right);
key_left = keyboard_check(vk_left);

key_action = keyboard_check(ord(','));
key_flash = keyboard_check(ord('.'));
key_ability = keyboard_check(ord('/'));
key_switchnorm = keyboard_check(ord('O'))
key_switchr = keyboard_check(ord('P'));
//ability laser
key_switchb = keyboard_check(ord('['));
//ability barrier
key_switchg = keyboard_check(ord(']'));
// ability slow





