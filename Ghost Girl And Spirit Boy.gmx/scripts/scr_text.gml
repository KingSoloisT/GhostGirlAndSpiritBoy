txt = instance_create(argument2,argument3,obj_text);
with (txt)

{
padding = 5;
text = argument0;
spd = argument1;
font = fnt1

text_length = string_length(text);
font_size = font_get_size(font);

draw_set_font(font);

}

// to use for spirit boy
// scr_text("insert text here ",0.2,2,215);
